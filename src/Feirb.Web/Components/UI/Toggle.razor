@namespace Feirb.Web.Components.UI

@* Switch toggle with optional separate label for the deactivated state. *@
<div class="form-check form-switch mb-0 @Class">
    <input type="checkbox" id="@_id" class="form-check-input"
           @bind="Value" @bind:after="OnToggleAsync"
           @attributes="AdditionalAttributes" />
    <label for="@_id" class="form-check-label @(Value ? "" : DisabledText is null ? "text-decoration-line-through text-muted" : "text-muted")">
        @(Value ? Text : DisabledText ?? Text)
    </label>
</div>

@code {
    /// <summary>Current toggle state.</summary>
    [Parameter]
    public bool Value { get; set; }

    /// <summary>Fires when the toggle state changes.</summary>
    [Parameter]
    public EventCallback<bool> ValueChanged { get; set; }

    /// <summary>Label text displayed when the toggle is on.</summary>
    [Parameter, EditorRequired]
    public string Text { get; set; } = default!;

    /// <summary>Optional label text displayed when the toggle is off. Falls back to Text if not set.</summary>
    [Parameter]
    public string? DisabledText { get; set; }

    /// <summary>Additional CSS classes.</summary>
    [Parameter]
    public string? Class { get; set; }

    /// <summary>Any extra HTML attributes.</summary>
    [Parameter(CaptureUnmatchedValues = true)]
    public Dictionary<string, object>? AdditionalAttributes { get; set; }

    private readonly string _id = $"toggle-{Guid.NewGuid():N}";

    private async Task OnToggleAsync() =>
        await ValueChanged.InvokeAsync(Value);
}
