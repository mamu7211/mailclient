@using Feirb.Web.Components.UI
@inject IJSRuntime JS

<InfoHeader Title="Button" Icon="hand-index-thumb" Subtitle="Standard button with optional icon and multiple variants" />

<Card Class="mb-3 p-4">
    <Heading Level="HeadingLevel.Small">Info</Heading>
    <p>Standard button with optional icon. Maps to Bootstrap button classes with consistent styling.</p>
    <Table Size="TableSize.Small">
        <TableHeader>
            <TableColumn>Parameter</TableColumn>
            <TableColumn>Type</TableColumn>
            <TableColumn>Default</TableColumn>
            <TableColumn>Description</TableColumn>
        </TableHeader>
        <TableBody>
            <TableRow><TableCell><code>Variant</code></TableCell><TableCell><code>ButtonVariant</code></TableCell><TableCell><code>Primary</code></TableCell><TableCell>Primary, Secondary, Danger, Warning</TableCell></TableRow>
            <TableRow><TableCell><code>Size</code></TableCell><TableCell><code>ButtonSize</code></TableCell><TableCell><code>Medium</code></TableCell><TableCell>Small, Medium, Large</TableCell></TableRow>
            <TableRow><TableCell><code>Icon</code></TableCell><TableCell><code>string?</code></TableCell><TableCell><code>null</code></TableCell><TableCell>Icon name (without <code>bi-</code> prefix)</TableCell></TableRow>
            <TableRow><TableCell><code>IconPosition</code></TableCell><TableCell><code>IconPosition</code></TableCell><TableCell><code>Left</code></TableCell><TableCell>Left or Right</TableCell></TableRow>
            <TableRow><TableCell><code>Disabled</code></TableCell><TableCell><code>bool</code></TableCell><TableCell><code>false</code></TableCell><TableCell>Disabled state</TableCell></TableRow>
            <TableRow><TableCell><code>ChildContent</code></TableCell><TableCell><code>RenderFragment?</code></TableCell><TableCell>&#8212;</TableCell><TableCell>Button text/content</TableCell></TableRow>
            <TableRow><TableCell><code>OnClick</code></TableCell><TableCell><code>EventCallback</code></TableCell><TableCell>&#8212;</TableCell><TableCell>Click handler</TableCell></TableRow>
        </TableBody>
    </Table>
</Card>

<Card Class="mb-3 p-4">
    <Heading Level="HeadingLevel.Small">Preview</Heading>
    <div class="d-flex flex-wrap align-items-center gap-3 mb-3">
        <label class="form-label mb-0">Variant:</label>
        <select class="form-select form-select-sm w-auto" @bind="_btnVariant">
            <option value="@ButtonVariant.Primary">Primary</option>
            <option value="@ButtonVariant.Secondary">Secondary</option>
            <option value="@ButtonVariant.Danger">Danger</option>
            <option value="@ButtonVariant.Warning">Warning</option>
        </select>
        <label class="form-label mb-0">Size:</label>
        <select class="form-select form-select-sm w-auto" @bind="_btnSize">
            <option value="@ButtonSize.Small">Small</option>
            <option value="@ButtonSize.Medium">Medium</option>
            <option value="@ButtonSize.Large">Large</option>
        </select>
        <label class="form-label mb-0">Icon:</label>
        <select class="form-select form-select-sm w-auto" @bind="_btnIcon">
            <option value="">(none)</option>
            <option value="pencil">pencil</option>
            <option value="trash">trash</option>
            <option value="download">download</option>
            <option value="plus-lg">plus-lg</option>
        </select>
        <div class="form-check">
            <input class="form-check-input" type="checkbox" id="btnDisabled" @bind="_btnDisabled" />
            <label class="form-check-label" for="btnDisabled">Disabled</label>
        </div>
    </div>
    <div class="showcase-preview-area">
        <Button Variant="@_btnVariant"
                Size="@_btnSize"
                Icon="@(string.IsNullOrEmpty(_btnIcon) ? null : _btnIcon)"
                Disabled="@_btnDisabled">
            Save
        </Button>
    </div>
</Card>

<Card Class="p-4">
    <Heading Level="HeadingLevel.Small">Code</Heading>
    <pre><code class="language-xml" @ref="_codeElement"></code></pre>
</Card>

@code {
    private ButtonVariant _btnVariant = ButtonVariant.Primary;
    private ButtonSize _btnSize = ButtonSize.Medium;
    private string _btnIcon = "";
    private bool _btnDisabled;
    private ElementReference _codeElement;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        try { await JS.InvokeVoidAsync("highlightCode", _codeElement, CurrentCode); }
        catch (JSException) { }
    }

    private string CurrentCode
    {
        get
        {
            var attrs = "";
            if (_btnVariant != ButtonVariant.Primary) attrs += $" Variant=\"ButtonVariant.{_btnVariant}\"";
            if (_btnSize != ButtonSize.Medium) attrs += $" Size=\"ButtonSize.{_btnSize}\"";
            if (!string.IsNullOrEmpty(_btnIcon)) attrs += $" Icon=\"{_btnIcon}\"";
            if (_btnDisabled) attrs += " Disabled=\"true\"";
            return $"<Button{attrs}>Save</Button>";
        }
    }
}
